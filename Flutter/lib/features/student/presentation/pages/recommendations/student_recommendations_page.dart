import 'package:flutter/material.dart';

class StudentRecommendationsPage extends StatelessWidget {
  const StudentRecommendationsPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Student Recommendations',
      ),
    );
  }
}
