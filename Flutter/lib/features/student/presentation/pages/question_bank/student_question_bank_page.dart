import 'package:flutter/material.dart';

class StudentQuestionBankPage extends StatelessWidget {
  const StudentQuestionBankPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Student Question Bank',
      ),
    );
  }
}
