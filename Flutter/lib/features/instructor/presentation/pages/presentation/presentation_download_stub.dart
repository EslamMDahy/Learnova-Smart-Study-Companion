Future<void> exportPresentationPptx({
  required String deckJson,
  required String filename,
}) async {
  throw UnsupportedError(
    'Editable PowerPoint export is currently available on Flutter Web only.',
  );
}
